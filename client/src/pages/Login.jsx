import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ArrowLeft,ShieldCheck,Route,AlertTriangle,Eye,EyeOff} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);
async function submit(e){e.preventDefault();setError('');setBusy(true);try{await login({email,password});nav('/dashboard')}catch(err){setError(err.response?.data?.msg||err.response?.data?.error||'Unable to sign in. Check your details and try again.')}finally{setBusy(false)}}return <><header className="navbar"><div className="nav-inner"><Link to="/" className="brand"><img src="/travaio_nobg.png"/><span>Travaio</span></Link><Link to="/" className="sign-in"><ArrowLeft size={15}/> Back to Home</Link></div></header><main className="auth-page"><div className="auth-grid"><div className="auth-card"><h2>Welcome back</h2><p>Sign in to access your travel dashboard.</p>{error&&<div className="form-error">{error}</div>}<form onSubmit={submit}><div className="form-group"><label>Email address</label><input className="form-control" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></div><div className="form-group"><label>Password</label><div style={{position:'relative'}}><input className="form-control" style={{paddingRight:45}} type={show?'text':'password'} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/><button type="button" onClick={()=>setShow(!show)} style={{position:'absolute',right:10,top:10,border:0,background:'none',color:'#718184'}}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div><button className="primary-btn" style={{width:'100%'}} disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><p className="center-note">New to Travaio? <Link to="/signup" style={{color:'var(--teal)',fontWeight:700}}>Create an account</Link></p></div><div className="auth-side"><span className="eyebrow">TRAVEL WITH CONFIDENCE</span><h1>Your journey deserves a second pair of eyes.</h1><p>Travaio monitors the journey in the background and asks for your attention only when something meaningful changes.</p><div className="check-list"><Check icon={<ShieldCheck/>} text="Route-aware safety monitoring"/><Check icon={<Route/>} text="Expected route and live journey context"/><Check icon={<AlertTriangle/>} text="Escalation when safety reminders go unanswered"/></div></div></div></main></>}
function Check({icon,text}){return <div className="check">{icon}<span>{text}</span></div>}